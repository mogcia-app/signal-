rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 開発・テスト用：一時的にすべてのアクセスを許可
    // 本番環境では適切な認証ルールに変更する必要があります
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
